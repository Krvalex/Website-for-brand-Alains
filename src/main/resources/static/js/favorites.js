function removeFavoriteItem(button) {
    const itemId = button.getAttribute('data-id');
    // Здесь должна быть реализация запроса на сервер для удаления из избранного
    console.log(`Удалить элемент с ID: ${itemId}`);
}
